# Compact-route calibration

Status: current v3 compact-route authority contract.

Normative: Yes — CORPUS-001, THRESH-BOOT-001, and independent compact-route promotion.
Implemented: Yes. Corpus generation, authority preparation, foundation bootstrap, fresh family
calibration, reuse calibration, and promoted authority closure are complete.

The runtime package retains only the verified, embedded authority closure; development corpus,
calibration tooling, and their large report artifacts are intentionally not distributed. The four
compact (`perfectpixel.svg-editable/1`) families remain independently bound to that authority. The
bounded-illustration compact entry binds `stable-solid-illustration` v2,
`perfectpixel.vector-evaluation/3`, its report digest, and adaptive policy/selector/trace/work-limit
closure. Motion routes remain `candidateShadow` and are not calibration candidates.

## Evidence closure

The completed authority closure validates corpus generator `vector-transition-corpus/2`, all v2
lock identities (including source, defect, split, license, generator-source, schema, and
unique-artifact inventories), immutable holdout, and the sole machine-pass foundation bootstrap.
Foundation evidence is a universal prerequisite, not a family-promotion waiver.

Each promoted family bundle is independent and acyclic. It contains exactly one family, its backend
`id`, `version`, and canonical backend-source digest, evaluation report schema v3 and report digest,
numeric effective thresholds and fixed gates, the complete corpus/holdout/bootstrap identity closure
(including `resultMatrixDigest`), applicable source and controlled-defect inventories, and
`noWaivers: true`. The bounded-illustration bundle additionally binds the adaptive
policy/selector/trace identities and hard work limits.

Every applicable source fixture was exercised through `vector-analyze` and `vector`, with
deterministic analysis and truthful supported/conditional outcomes. Controlled-defect PNGs were
never treated as source requests: calibration invoked the exact cfg-test-only quality probe against
each locked parent/defect pair twice, under the shared zero-budget protected or declared resource
gate. The shipped promotion manifest retains only the derived identities and `machine-pass`
decision; development input and report artifacts are not part of this checkout.

Fresh and reuse calibration both revalidate corpus, bootstrap, persisted results, bundle closure,
manifest closure, and current registry/entry identities. The completed promotion manifest binds
each compact key to its exact current route entry digest, predecessor digest, family bundle
digest, persisted evaluation/review hashes, and `machine-pass` decision. Runtime verifies the
embedded v2 family bundles and `promotion.json` against the current route registry and entries.

## Completed authority closure

The completed dependency order was corpus generation, authority preparation, foundation bootstrap,
fresh family calibration, and reuse calibration. The resulting authority is content-addressed; the
bounded-illustration compact route is promoted only after every closure check passes, and a failed
closure leaves the affected route unsupported.
